# Hora Homebrew Tap

Install the latest direct release of hora Calendar:

```sh
brew install --cask szamowski-dev/tap/hora
```

The cask uses an immutable, signed and notarized ZIP. App updates are delivered
by hora Calendar through Sparkle.
